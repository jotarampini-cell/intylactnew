import PageShell from "@/components/layout/PageShell";

export const metadata = { title: "Blog" };

export default function BlogPage() {
  return (
    <PageShell
      title="Blog"
      intro="Contenido sobre salud íntima, ciclo menstrual y bienestar femenino."
    />
  );
}
